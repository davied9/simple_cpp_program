stages:
    - build
    - test
    
build_linux:
    stage: build
    tags:  build_linux
    script:
        - python -m build -vv install
        
build_windows:
    stage: build
    tags:  build_windows
    script:
        - python -m build -vv install
        
test:
    stage: test
    script:
        - cd pacages
        - ./simple_cpp_program
        
artifacts:
    path: packages
    expired_in: 10 days
    